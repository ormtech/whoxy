require "spec_helper"

describe Jsonwhois do
  it "has a version number" do
    expect(Jsonwhois::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
